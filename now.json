{
    "version": 2,
    "name": "swiper",
    "builds": [{ "src": "swiper/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "swiper/$1" }
    ]
}